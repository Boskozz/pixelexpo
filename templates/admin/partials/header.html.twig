<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('admin_dashboard') }}">Administration</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            {% set entite = app.request.get('_route') | split('_') %}
            <li class="nav-item">
                <a class="nav-link {% if entite[1] == "dashboard" %}active{% endif %}" href="{{ path('admin_dashboard') }}">Tableau de bord</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if entite[1] == "user" %}active{% endif %}" href="{{ path('admin_user_index') }}">Utilisateurs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if entite[1] == "projet" %}active{% endif %}" href="{{ path('admin_projet_index') }}">Projets</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if entite[1] == "album" %}active{% endif %}" href="{{ path('admin_album_index') }}">Albums</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if entite[1] == "etiquette" %}active{% endif %}" href="{{ path('admin_etiquette_index') }}">Etiquettes</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto text-white">
            <li class="nav-item">
                <a href="{{ path('user_dashboard') }}" class="nav-link {% if app.request.get('_route') == "user_dashboard" %}active{% endif %}">{{ app.user.username }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepage') }}">Retour au site</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
            </li>
        </ul>
    </div>
</nav>
{% for label, messages in app.flashes %}
    <div class="container mt-2">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}