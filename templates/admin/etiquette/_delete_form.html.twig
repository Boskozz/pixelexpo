<form method="post" action="{{ path('admin_etiquette_delete', {'id': etiquette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etiquette.id) }}">
    <button class="btn btn-outline-danger btn-sm">Supprimer</button>
</form>
