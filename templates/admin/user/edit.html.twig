{% extends 'admin/base.html.twig' %}

{% block title %}Edition d'un utilisateur{% endblock %}

{% block body %}
    <div class="container my-3">
        <h1>Edition d'un utilisateur</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-outline-success">Valider la modification</button>
        <a href="{{ path('admin_user_index') }}" class="btn btn-outline-secondary">Liste utilisateurs</a>
        {{ form_end(form) }}
    </div>
{% endblock %}