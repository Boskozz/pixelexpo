{% extends 'admin/base.html.twig' %}

{% block title %}Edition de l'album : {{ album.title }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-10">
                <h2>Edition de l'album : {{ album.title }} de {{ album.projet.author.username }}</h2>
            </div>
            <div class="col-md-2">

            </div>
        </div>
        {{ include('admin/album/_form.html.twig', {'button_label': 'Valider la modification'}) }}
    </div>
{% endblock %}
