{% extends 'public.html.twig' %}

{% block title %}
    Tableau de bord
{% endblock %}

{% block body %}
    <div class="container-fluid mt-3">
        <div class="row">
            <div class="col-md-4">
                <table class="table table-lignt table-striped">
                    <tr>
                        <td>Nom d'utilisateur</td>
                        <td>{{ user.username }}</td>
                    </tr>
                    <tr>
                        <td>Prénom</td>
                        <td>{{ user.firstName }}</td>
                    </tr>
                    <tr>
                        <td>Nom</td>
                        <td>{{ user.lastName }}</td>
                    </tr>
                    <tr>
                        <td>Email</td>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <td>Membre depuis le</td>
                        <td>{{ user.createdAt|date('d/m/Y') }}</td>
                    </tr>
                    <tr>
                        <td>Profil maj le</td>
                        <td>{{ user.updatedAt|date('d/m/Y') }}</td>
                    </tr>
                </table>
            </div>
            <div class="col-md-4">
                <a href="{{ path('public_projet', {username: user.username}) }}" class="btn btn-outline-primary">Projets publics de {{ user.username|capitalize }}</a>
            </div>
        </div>
    </div>
{% endblock %}


